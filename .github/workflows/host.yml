name: Upload Website

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: jakejarvis/s3-sync-action@v0.5.1
        with:
          args: --acl public-read --follow-symlinks --delete
        env:
          AWS_S3_BUCKET: deployauto
          AWS_ACCESS_KEY_ID: AKIA2UC3AOSEFFKKE46K
          AWS_SECRET_ACCESS_KEY: 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
